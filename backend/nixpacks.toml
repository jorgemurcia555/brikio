# Nixpacks configuration for Railway
# Railway will automatically detect this and use Nixpacks builder

[phases.setup]
nixPkgs = ["nodejs_20", "npm"]

[phases.install]
cmds = ["npm install --prefer-offline --no-audit"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm run start:prod"

